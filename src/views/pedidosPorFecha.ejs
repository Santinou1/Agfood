<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pedidos del <%= fecha %></title> <!-- Título de la página con la fecha -->
    <link rel="stylesheet" href="/styles.css"> <!-- Enlace al archivo de estilos -->
</head>
<body>
     <!-- Botón para volve -->
    <a href="/">
    <svg xmlns="http://www.w3.org/2000/svg" width="50px" height="50px" viewBox="0 0 24 24" fill="none">
        <circle cx="12" cy="12" r="12" fill="#28a745"><script xmlns=""/></circle>
        <path d="M14.5 17L9.5 12L14.5 7" stroke="#ffffff" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>

        
    </a>
    <div class="container">
    <h1>Pedidos del <%= fecha %></h1> <!-- Encabezado con la fecha de los pedidos -->
    <ul>
        <% pedidos.forEach(pedido => { %> <!-- Iterar sobre cada pedido -->
            <li>
                <strong>Nombre:</strong> <%= pedido.nombre %><br> <!-- Mostrar el nombre del cliente -->
                <strong>Comida:</strong> <%= pedido.comida.join(', ') %> <!-- Mostrar las comidas seleccionadas -->
            </li>
            <hr>
        <% }) %>
    </ul>
    <a href="/api/pedidos/descargarExcel/<%= fecha %>">
        <button>Exportar a Excel</button> <!-- Botón para exportar a Excel -->
    </a>
</div>
</body>
</html>